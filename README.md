# luxresume

A simple CLI to generate ATS-friendly PDF resumes from YAML files.

## Installation

    pip install luxresume

## Usage

To convert `resume.yaml` and output the result to `resume.pdf`:

    luxresume resume.yaml
